{
  "name": "cypress-cucumber-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^10.0.2",
    "@cypress/browserify-preprocessor": "^3.0.2",
    "cypress": "^9.6.1"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": "cypress/integration/stepDefinitions/**/*.{js,ts}"
  }
}
